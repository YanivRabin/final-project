/** @type {import('next').NextConfig} */
import withVideos from 'next-videos';

const nextConfig = {
  // Any other Next.js configuration options you need
  reactStrictMode: true, // Example option
};

export default withVideos(nextConfig);
